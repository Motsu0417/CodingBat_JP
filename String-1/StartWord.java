/*
【問題】
文字列[str][word]が与えられます。
[str]の先頭と[word]が一致すれば[str]の一致部分、一致しなければ空文字[""]を返してください。
ただし、[word]の1文字目は一致しなくともよいものとし、例えば["hippo"]に対し["hi"]なら["hi"]、["_ip"]なら["hip"]を返します。
なお、[word]は1文字以上の文字列です。

【例】
startWord("hippo", "hi") → "hi"
startWord("hippo", "xip") → "hip"
startWord("hippo", "i") → "h"

【コンパイル＆回答チェック方法】
> java Test StartWord
*/

public class StartWord{
	public static String startWord(String str, String word) {
		// ここに回答を記入
		
	}
}
